# Руководство по созданию видео с ИИ

[Открыть сайт](https://video-education-production.up.railway.app) · [Библиотека примеров](https://video-education-production.up.railway.app/#examples)

Первая HTML-версия учебного материала на основе заметок от 4 сентября 2026 года. Тёмное оформление, восемь уроков и отдельный экран с библиотекой примеров. Учебный текст отредактирован и дополнен практическими заданиями. Исходный Markdown с сообщениями сохранён отдельно.

## Файлы

- `index.html` — учебные материалы, оформление и встраивание постов X. Открывается как отдельный файл в браузере.
- `assets/discussions.js` и `assets/discussions.css` — формы вопросов, ветки ответов и кабинет автора.
- `server.mjs` и `src/` — сервер сайта и API обсуждений на Node.js.
- `migrations/` — версии схемы PostgreSQL; `BACKEND.md` описывает API и настройку сервера.
- `backup/` — ежедневное резервное копирование PostgreSQL в закрытое хранилище Railway.
- `package.json` и `railway.json` — параметры запуска на Railway.
- `PLAN.md` — согласованный план разработки по этапам.
- `PROGRESS.md` — текущее состояние, результаты проверок и следующий шаг.

## Локальный запуск

Для сервера нужны Node.js 22 или новее и PostgreSQL. Настройте `.env` по инструкции в `BACKEND.md`, затем выполните:

```sh
npm ci
node --env-file=.env scripts/migrate.mjs
npm run dev
```

Откройте `http://localhost:4173`. Учебные материалы можно читать и при открытии `index.html` напрямую; обсуждения требуют сервера. Для постов из X требуется интернет.

## Добавление постов из X

Можно присылать ссылки помощнику вместе с коротким пояснением, что показывается в ролике. Он добавит пост в подборку и свяжет его с главой.

Для ручного изменения найдите в конце `index.html`:

```html
<script id="x-examples" type="application/json">
```

Между тегами хранится массив объектов со следующими полями:

| Поле | Содержание |
| --- | --- |
| `url` | Настоящая ссылка вида `https://x.com/имя/status/числовой_ID` |
| `lesson` | Идентификатор главы из списка ниже |
| `title` | Название примера |
| `author` | Имя автора и дата поста |
| `watchFor` | На какие детали видео смотреть |
| `practice` | Какое действие предложить ученику |

Идентификаторы глав: `idea`, `first-frame`, `character`, `prompt`, `motion`, `storyboard`, `generation`, `editing`.

Посты появляются на экране «Библиотека примеров» по адресу `/#examples`. Каждый разбор содержит ссылку на свою главу. Порядок в массиве определяет порядок на странице. Уже добавлены два разных поста Reid Hannaford, которые предоставил автор курса:

- [Сложная сцена из простых форм — 25 июня 2026](https://x.com/reidhannaford/status/2070145120658137385)
- [Постановка сцены засады — 29 июня 2026](https://x.com/reidhannaford/status/2071595581508563168)

Повтор первого поста исключён. Оба поста подтверждены через официальный oEmbed X. Комментарии основаны на предоставленном тексте постов; подробный разбор самих видео можно расширить отдельно.

Используется официальный виджет X. Пост сохраняет авторство и встроенные видео или фотографии. Виджеты загружаются по мере приближения к ним при прокрутке. Видео не скачиваются на сервер. Если пост удалён, закрыт или X недоступен, остаётся ссылка на оригинал. Перед публикацией проверяйте примеры в браузере.

В JSON используйте обычный текст. Последовательность `</script>` в текстовом поле нужно записывать как `<\/script>`, чтобы не закрыть HTML-тег раньше времени. Ссылка проверяется по домену и формату ID; произвольный HTML в поля вставлять не нужно.

## Размещение на Railway

1. Исходный код хранится в [SlowMercury/video-education](https://github.com/SlowMercury/video-education), основная ветка — `main`.
2. Проект Railway называется `video-creation`; веб-сервис подключается к этому репозиторию.
3. Запуск задан в `railway.json`: перед публикацией выполняется `npm run migrate`, затем сервер запускается через `npm start`.
4. После успешного запуска создайте публичный домен в настройках сервиса.

Сервер слушает `0.0.0.0` и порт из переменной `PORT`, которую задаёт Railway. Проверка готовности доступна по `/healthz`; маршруты обсуждений начинаются с `/api/`. Исходный код, документация и секреты через сервер не раздаются.

Первая версия предназначена для открытого доступа по ссылке.

Под каждым из восьми уроков и каждым примером есть форма вопроса или комментария. Посетитель вводит имя и сообщение; регистрация не нужна. Все сообщения видны публично. Имя запоминается в этом браузере, но не подтверждает личность. Комментарии хранятся в PostgreSQL, отдельно для каждого урока и поста X.

Чтобы ответить конкретному участнику, нажмите «Ответить» под его сообщением. Текст ответа можно изменить или отменить ответ, сохранив черновик. Если соединение пропало, текст остаётся в форме: нажмите «Опубликовать» ещё раз после восстановления связи. Кнопка «Обновить» загружает начало обсуждения заново; «Показать ещё» добавляет следующие сообщения.

Автор входит через ссылку [«Вход для автора»](https://video-education-production.up.railway.app/#owner) внизу страницы. После входа его сообщения получают отметку «Автор курса», а под комментариями появляется «Удалить». Удаление требует подтверждения; ответы на удалённое сообщение сохраняются. В кабинете есть кнопка выхода. Пароль хранится отдельно от этого публичного репозитория.

Публикацией обсуждений управляет переменная `DISCUSSIONS_ENABLED=true`. Состояние развёртывания и следующий шаг записаны в `PROGRESS.md`.

## Официальная документация

- [Встраивание постов X](https://docs.x.com/x-for-websites/embedded-posts/overview)
- [JavaScript-функция создания виджета X](https://docs.x.com/x-for-websites/embedded-posts/guides/embedded-post-javascript-factory-function)
- [Посты с видео и ограничения доступа](https://help.x.com/en/using-x/how-to-embed-a-post)
- [Проверки готовности и PORT на Railway](https://docs.railway.com/deployments/healthchecks)
